<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="cn.testinone.DrawerActivity">

    <!--　
    DrawerLayout的第一个子元素是主要内容，即抽屉没有打开时显示的布局。这里采用了一个FrameLayout，里面什么也没放。
    DrawerLayout的第二个子元素是抽屉中的内容，即抽屉布局，这里采用了一个ListView。
    -->
    <android.support.v4.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/drawer_layout">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/content_frame">

            <Button
                android:id="@+id/btnOpenFrame"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="open"/>

        </FrameLayout>

        <!--layout_gravity="left" 表示从左边打开抽屉-->
        <ListView
            android:layout_width="240dp"
            android:layout_height="match_parent"
            android:layout_gravity="left"
            android:background="#FFF"
            android:divider="@android:color/transparent"
            android:dividerHeight="0dp">

        </ListView>
    </android.support.v4.widget.DrawerLayout>

</RelativeLayout>
